# FILES
# list of SNPtest result files (formatted for gwasqc)

FILES=`ls -1 ../Association/20130829/out/LNEGFR_CYCR_BASELINE/adjusted/*.gwas`

# DATA DIR
DIR_PREFIX="/data/gwas/pediGFR_gwas"
DATA_DIR="${DIR_PREFIX}/Mantra/20131009-prepare/out"

# Study count filter
# (only include SNPs in meta analysis that are present at least in MIN_STUDIES studies)
MIN_STUDIES=4

# Number of lines per Mantra data file
# Must be divisible by (num_studies + 1)
LINE_COUNT=3528000
